<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
	xmlns:tools="http://schemas.android.com/tools"
	package="com.cooder.cooder.app">
	
	<uses-permission android:name="android.permission.INTERNET" />
	
	<application
		android:name=".application.CoApplication"
		android:allowBackup="true"
		android:dataExtractionRules="@xml/data_extraction_rules"
		android:fullBackupContent="@xml/backup_rules"
		android:icon="@mipmap/ic_launcher"
		android:label="@string/app_name"
		android:roundIcon="@mipmap/ic_launcher_round"
		android:supportsRtl="true"
		android:theme="@style/Theme.CooderLibrary"
		android:usesCleartextTraffic="true"
		tools:targetApi="31">
		<activity
			android:name=".ui.MainActivity"
			android:exported="true">
			<intent-filter>
				<action android:name="android.intent.action.MAIN" />
				
				<category android:name="android.intent.category.LAUNCHER" />
			</intent-filter>
		</activity>
		<activity android:name=".ui.test.TestActivity" />
		<activity android:name=".ui.log.CoLogActivity" />
		<activity android:name=".ui.tab.CoTabBottomActivity" />
		<activity android:name=".ui.tab.CoTabTopActivity" />
		<activity android:name=".ui.refresh.CoRefreshActivity" />
		<activity android:name=".ui.banner.CoBannerActivity" />
		<activity android:name=".ui.item.CoDataItemActivity" />
		<activity android:name=".ui.slider.CoSliderActivity" />
		<activity android:name=".ui.databus.CoDataBusActivity" />
	</application>

</manifest>